// TODO: NotificationsController — /api/v1/notifications
// All routes protected by FirebaseAuthGuard + RolesGuard
//
// GET    /notifications          → getAll()       [ALL_ROLES]
// GET    /notifications/:id      → getOne()       [ALL_ROLES]
// POST   /notifications          → create()       [ALL_ROLES]
// PATCH  /notifications/:id      → update()       [ALL_ROLES]
// DELETE /notifications/:id      → remove()       [DISTRICT_ADMIN, FACILITY_ADMIN]
