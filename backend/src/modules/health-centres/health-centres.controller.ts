// TODO: HealthCentresController — /api/v1/health-centres
// All routes protected by FirebaseAuthGuard + RolesGuard
//
// GET    /health-centres          → getAll()       [DISTRICT_ADMIN]
// GET    /health-centres/:id      → getOne()       [DISTRICT_ADMIN]
// POST   /health-centres          → create()       [DISTRICT_ADMIN]
// PATCH  /health-centres/:id      → update()       [DISTRICT_ADMIN]
// DELETE /health-centres/:id      → remove()       [DISTRICT_ADMIN, FACILITY_ADMIN]
